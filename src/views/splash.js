import React from 'react';
import {StyleSheet, View} from 'react-native';
import {SplashImage} from '../assets/svgs';

const Splash = () => {
  return (
    <View style={{flex: 1}}>
      <SplashImage />
    </View>
  );
};

const styles = StyleSheet.create({});

export default Splash;
